apiVersion: v1
kind: Service
metadata:
  name: {{ include "back-service.serviceAccountName" . }}
  labels:
    {{- include "back-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "back-service.selectorLabels" . | nindent 4 }}
